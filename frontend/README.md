# Luxery CRM – Frontend

React + Vite + TypeScript SPA. See the [root README](../README.md) for how to run the project and system overview.

```bash
cp .env.example .env   # VITE_API_BASE_URL=http://localhost:8000/api/v1
npm install
npm run dev
```
